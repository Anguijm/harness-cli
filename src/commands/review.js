import chalk from 'chalk';

export async function review(file, options) {
  console.log(chalk.dim('\n  harness review — coming in v0.2'));
  console.log(chalk.dim('  Will run council on existing code files.\n'));
}
